import 'package:flutter/material.dart';

class Screening3Page extends StatelessWidget {
  const Screening3Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
